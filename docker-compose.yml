services:
  jarvis:
    build: .
    container_name: jarvis
    restart: unless-stopped
    ports:
      - "8088:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    mem_limit: 512m
    memswap_limit: 512m
